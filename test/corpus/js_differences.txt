================================================================================
var is not a keyword — produces ERROR
================================================================================

var x = 1;

--------------------------------------------------------------------------------

(program
  (expression_statement
    (assignment_expression
      left: (identifier)
      (ERROR (identifier))
      right: (number))))

================================================================================
new is not a keyword — produces ERROR
================================================================================

new Foo();

--------------------------------------------------------------------------------

(program
  (ERROR (identifier))
  (expression_statement
    (call_expression
      function: (identifier)
      arguments: (arguments))))

================================================================================
throw is not a keyword — produces ERROR
================================================================================

throw err;

--------------------------------------------------------------------------------

(program
  (expression_statement
    (identifier)
    (ERROR (identifier))))

================================================================================
typeof is not a keyword — produces ERROR
================================================================================

typeof x;

--------------------------------------------------------------------------------

(program
  (expression_statement
    (identifier)
    (ERROR (identifier))))

================================================================================
Array destructuring — produces ERROR
================================================================================

let [a, b] = arr;

--------------------------------------------------------------------------------

; Ucode has no destructuring. `let [a, b] = arr` is not valid ucode.
; `let` is a keyword and cannot be used as an identifier, so the parser
; produces an ERROR node. The `= arr` portion is recovered as a separate
; expression statement.

(program
  (ERROR
    (array
      (identifier)
      (identifier)))
  (expression_statement
    (identifier)))

================================================================================
Object destructuring — produces ERROR
================================================================================

let {x} = obj;

--------------------------------------------------------------------------------

(program
  (ERROR)
  (statement_block
    (expression_statement (identifier)))
  (ERROR)
  (expression_statement (identifier)))

================================================================================
class is not a keyword — produces ERROR
================================================================================

class Foo {}

--------------------------------------------------------------------------------

(program
  (ERROR
    (identifier)
    (identifier))
  (statement_block))

================================================================================
do-while is not supported — produces ERROR
================================================================================

do { x++; } while (x < 10);

--------------------------------------------------------------------------------

(program
  (ERROR (identifier))
  (statement_block
    (expression_statement
      (update_expression argument: (identifier))))
  (while_statement
    condition: (parenthesized_expression
      (binary_expression left: (identifier) right: (number)))
    body: (empty_statement)))

================================================================================
for...of is not supported — produces ERROR
================================================================================

for (x of arr) {}

--------------------------------------------------------------------------------

(program
  (ERROR
    (ERROR
      (identifier)
      (identifier)
      (identifier))
    (object)))

================================================================================
async function is not supported — produces ERROR
================================================================================

async function f() {}

--------------------------------------------------------------------------------

(program
  (ERROR (identifier))
  (function_declaration
    name: (identifier)
    parameters: (formal_parameters)
    body: (statement_block)))

================================================================================
Generator function is not supported — produces ERROR
================================================================================

function* gen() {}

--------------------------------------------------------------------------------

; `*` is not valid between `function` and the name; it lands as an ERROR node
; inside the function_declaration while the rest parses normally.

(program
  (function_declaration
    (ERROR)
    name: (identifier)
    parameters: (formal_parameters)
    body: (statement_block)))

================================================================================
>>> unsigned right shift is not supported — produces ERROR
================================================================================

let x = a >>> b;

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (binary_expression
        left: (identifier)
        (ERROR)
        right: (identifier)))))

================================================================================
void is not a keyword — produces ERROR
================================================================================

void x;

--------------------------------------------------------------------------------

(program
  (expression_statement
    (identifier)
    (ERROR (identifier))))

================================================================================
instanceof is not a keyword — produces ERROR
================================================================================

a instanceof b;

--------------------------------------------------------------------------------

(program
  (ERROR
    (identifier)
    (identifier))
  (expression_statement (identifier)))

================================================================================
debugger is not a keyword — silently parses as identifier
================================================================================

debugger;

--------------------------------------------------------------------------------

; Unlike other removed JS keywords, `debugger` is not in the reserved list and
; parses without error as a plain expression statement containing an identifier.

(program
  (expression_statement (identifier)))

================================================================================
await is not a keyword — produces ERROR
================================================================================

await foo();

--------------------------------------------------------------------------------

(program
  (expression_statement
    (call_expression
      function: (identifier)
      (ERROR (identifier))
      arguments: (arguments))))

================================================================================
yield is not a keyword — produces ERROR
================================================================================

yield x;

--------------------------------------------------------------------------------

(program
  (expression_statement
    (identifier)
    (ERROR (identifier))))

================================================================================
Bare array destructuring assignment — produces ERROR
================================================================================

[a, b] = [1, 2];

--------------------------------------------------------------------------------

; Ucode has no destructuring. A bare `[a, b] = [...]` is parsed as a subscript
; expression where `[a, b]` is the object and `= [...]` is invalid inside the
; subscript, so the parser produces an ERROR node around the `=` and its operand.

(program
  (expression_statement
    (subscript_expression
      object: (array
        (identifier)
        (identifier))
      (ERROR)
      index: (sequence_expression
        (number)
        (number)))))

================================================================================
Function parameter destructuring — produces ERROR
================================================================================

function f([a, b]) {}

--------------------------------------------------------------------------------

(program
  (ERROR
    (identifier)
    (array
      (identifier)
      (identifier)))
  (statement_block))

================================================================================
Dynamic import — produces ERROR
================================================================================

import("module");

--------------------------------------------------------------------------------

(program
  (ERROR)
  (expression_statement
    (parenthesized_expression
      (string
        (string_fragment)))))

================================================================================
Numeric underscore separators are not supported — produces ERROR
:error
================================================================================

let x = 1_000;

--------------------------------------------------------------------------------

================================================================================
Leading-dot floats are not supported — produces ERROR
:error
================================================================================

let x = .5;

--------------------------------------------------------------------------------

================================================================================
Legacy octal with a fraction is invalid — produces ERROR
:error
================================================================================

let x = 0177.5;

--------------------------------------------------------------------------------

================================================================================
Legacy octal with an exponent is invalid — produces ERROR
:error
================================================================================

let x = 0177e2;

--------------------------------------------------------------------------------

================================================================================
Leading-zero integer with a non-octal digit is invalid — produces ERROR
:error
================================================================================

let x = 019;

--------------------------------------------------------------------------------

================================================================================
const without initializer — produces ERROR
:error
================================================================================

const a;

--------------------------------------------------------------------------------

================================================================================
delete on a non-property expression — produces ERROR
:error
================================================================================

delete a;

--------------------------------------------------------------------------------

================================================================================
Array element elision is not supported — produces ERROR
:error
================================================================================

let x = [1, , 2];

--------------------------------------------------------------------------------

================================================================================
Object element elision is not supported — produces ERROR
:error
================================================================================

let x = { a: 1, , b: 2 };

--------------------------------------------------------------------------------

================================================================================
Call-argument elision is not supported — produces ERROR
:error
================================================================================

print(1, , 2);

--------------------------------------------------------------------------------

================================================================================
Trailing comma in call arguments is not supported — produces ERROR
:error
================================================================================

print(1, 2,);

--------------------------------------------------------------------------------

================================================================================
Trailing comma in named import is not supported — produces ERROR
:error
================================================================================

import { a, } from "./m.uc";

--------------------------------------------------------------------------------

================================================================================
Trailing comma in export clause is not supported — produces ERROR
:error
================================================================================

export { z, };

--------------------------------------------------------------------------------

================================================================================
export function requires a trailing semicolon — produces ERROR
:error
================================================================================

export function f() {}

--------------------------------------------------------------------------------

================================================================================
Braced unicode escape \u{...} is not supported in strings — produces ERROR
:error
================================================================================

let s = "\u{1F600}";

--------------------------------------------------------------------------------

================================================================================
Unicode escapes in identifiers are not supported — produces ERROR
:error
================================================================================

let \u0041 = 1;

--------------------------------------------------------------------------------

================================================================================
Dollar sign is not an identifier character — produces ERROR
:error
================================================================================

let $x = 1;

--------------------------------------------------------------------------------

================================================================================
Non-ASCII letters are not identifier characters — produces ERROR
:error
================================================================================

let café = 1;

--------------------------------------------------------------------------------

================================================================================
Rest parameter must be last — produces ERROR
:error
================================================================================

function f(...a, b) {}

--------------------------------------------------------------------------------

================================================================================
Only one rest parameter is allowed — produces ERROR
:error
================================================================================

function f(...a, ...b) {}

--------------------------------------------------------------------------------

================================================================================
Binary operator directly before a ternary qmark — produces ERROR
:error
================================================================================

x = a -? b : c;

--------------------------------------------------------------------------------

================================================================================
Division slash directly before a ternary qmark — produces ERROR
:error
================================================================================

x = a /? b : c;

--------------------------------------------------------------------------------

================================================================================
Newline before a ternary qmark is allowed
================================================================================

let r = x
? a : b;

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (ternary_expression
        condition: (identifier)
        consequence: (identifier)
        alternative: (identifier)))))

================================================================================
delete accepts a parenthesized property access
================================================================================

delete (o.a);

--------------------------------------------------------------------------------

(program
  (expression_statement
    (delete_expression
      argument: (parenthesized_expression
        (member_expression
          object: (identifier)
          property: (property_identifier))))))

================================================================================
Parenthesized assignment target is rejected — produces ERROR
:error
================================================================================

(a) = 2;

--------------------------------------------------------------------------------

================================================================================
Increment of a non-reference operand is rejected — produces ERROR
:error
================================================================================

++5;

--------------------------------------------------------------------------------

================================================================================
Lexical declaration as an unbraced control body — produces ERROR
:error
================================================================================

if (x) let y = 1;

--------------------------------------------------------------------------------

================================================================================
import as an unbraced control body is not supported — produces ERROR
:error
================================================================================

if (x) import "m.uc";

--------------------------------------------------------------------------------

================================================================================
export as an unbraced control body is not supported — produces ERROR
:error
================================================================================

if (x) export { y };

--------------------------------------------------------------------------------

================================================================================
try with finally is not supported — produces ERROR
:error
================================================================================

try { a(); } finally { b(); }

--------------------------------------------------------------------------------

================================================================================
Re-export with from is not supported — produces ERROR
:error
================================================================================

export { a } from "./m.uc";

--------------------------------------------------------------------------------

================================================================================
No ASI for the initializer in a C-style for-header — produces ERROR
:error
================================================================================

; ucode (like ECMAScript) never inserts a semicolon inside a for-header; the
; `;` after the initializer is mandatory. A bare newline is an error.

for (let i = 0
i < 3; i++) {}

--------------------------------------------------------------------------------

================================================================================
const is not allowed in a C-style for-header initializer — produces ERROR
:error
================================================================================

; ucode accepts only `let` (or a bare expression) as a for-header initializer;
; `const` is rejected ("Expecting expression"), same as for the for-in target.

for (const c = 0; c < 3; ) {}

--------------------------------------------------------------------------------

================================================================================
Lenient ASI: a bare newline separates two statements (divergence — see README)
================================================================================

x = 1
y = 2

--------------------------------------------------------------------------------

(program
  (expression_statement
    (assignment_expression
      left: (identifier)
      right: (number)))
  (expression_statement
    (assignment_expression
      left: (identifier)
      right: (number))))

================================================================================
Lenient ASI: a bare newline separates two lexical declarations (divergence)
================================================================================

let a = 1
let b = 2

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (number)))
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (number))))

================================================================================
Lenient ASI across a line comment on its own line (divergence)
================================================================================

x = 1
// c
y = 2

--------------------------------------------------------------------------------

(program
  (expression_statement
    (assignment_expression
      left: (identifier)
      right: (number))
    (comment))
  (expression_statement
    (assignment_expression
      left: (identifier)
      right: (number))))

================================================================================
Line comment on its own line does not break member-chain continuation
================================================================================

let r = a
// c
.b;

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (member_expression
        object: (identifier)
        (comment)
        property: (property_identifier)))))

================================================================================
Block comment on its own line does not break binary-operator continuation
================================================================================

let r = a
/* c */
+ b;

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (binary_expression
        left: (identifier)
        (comment)
        right: (identifier)))))

================================================================================
Line comment on its own line does not break a leading-minus continuation
================================================================================

let r = a
// c
- b;

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (binary_expression
        left: (identifier)
        (comment)
        right: (identifier)))))

================================================================================
Exotic inline whitespace (form feed) after a newline keeps a member continuation
================================================================================

let r = a
.b;

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (member_expression
        object: (identifier)
        property: (property_identifier)))))

================================================================================
Exotic inline whitespace (form feed) after a newline keeps a binary continuation
================================================================================

let r = a
/* c */
+ b;

--------------------------------------------------------------------------------

(program
  (lexical_declaration
    (variable_declarator
      name: (identifier)
      value: (binary_expression
        left: (identifier)
        (comment)
        right: (identifier)))))
