{
  "name": "tree-sitter-ucode",
  "version": "0.3.0",
  "description": "Ucode grammar for tree-sitter",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/m00qek/tree-sitter-ucode.git"
  },
  "license": "MIT",
  "main": "bindings/node",
  "types": "bindings/node",
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "ucode",
    "openwrt"
  ],
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**",
    "tmpl/grammar.js",
    "tmpl/src/**",
    "tmpl/queries/*",
    "*.wasm"
  ],
  "dependencies": {
    "node-addon-api": "^8.3.1",
    "node-gyp-build": "^4.8.4"
  },
  "devDependencies": {
    "prebuildify": "^6.0.1",
    "tree-sitter-cli": "^0.26.0"
  },
  "peerDependencies": {
    "tree-sitter": "^0.22.0"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "scripts": {
    "install": "node-gyp-build",
    "build": "tree-sitter generate && tree-sitter generate tmpl/grammar.js --output tmpl/src && node-gyp-build && tree-sitter build --output ucode.so . && tree-sitter build --output ucode_tmpl.so ./tmpl",
    "test": "tree-sitter test --lib-path ucode.so --lang-name ucode && tree-sitter test -p tmpl",
    "prestart": "tree-sitter build --wasm",
    "start": "tree-sitter playground"
  }
}
